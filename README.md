# Liberia Connect

This project is a web application that connects users with local service providers in Liberia. It is built with React, TypeScript, Vite, and Tailwind CSS.

## Getting Started

1.  Install dependencies: `npm install`
2.  Start the development server: `npm run dev`
3.  Open the application in your browser at `http://localhost:5173`
